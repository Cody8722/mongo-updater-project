# MongoDB 連線字串（必需）
# 格式: mongodb+srv://<username>:<password>@<cluster>.mongodb.net/
MONGO_URI=

# 管理員密碼（選填，用於系統監控、統計資料、檔案管理等管理功能）
# 建議使用強密碼，例如：openssl rand -hex 32
ADMIN_SECRET=

# 壓縮工具服務 URL（選填，用於系統監控）
# 本地開發: http://localhost:5000
# 生產環境: https://zip.zeabur.app
COMPRESSOR_URL=https://zip.zeabur.app

# 班表工具服務 URL（選填，用於系統監控）
# 本地開發: http://localhost:3000
# 生產環境: https://shift-schedule-generators.zeabur.app
SCHEDULE_URL=https://shift-schedule-generators.zeabur.app

# 部署平台說明：
# - 本地開發：複製此檔案為 .env 並填入實際值
# - Zeabur：在環境變數設定中直接設定上述變數
# - Heroku：使用 heroku config:set MONGO_URI=your_value
# - Docker：在 docker-compose.yml 中設定環境變數
